const setAlgos = require("./monday.js");

test("intersect unordered sets", () => {
    expect(1+1).toBe(2);
});

test("intersect unordered multiset", () => {
    expect(1+1).toBe(2);
})

test("intersect ordered sets", () => {
    expect(1+1).toBe(2);
});

test("intersect ordered multiset", () => {
    expect(1+1).toBe(2);    
})

test("union unordered sets", () => {
    expect(1+1).toBe(2);
});

test("union unordered multiset", () => {
    expect(1+1).toBe(2);    
})

test("union ordered sets", () => {
    expect(1+1).toBe(2);
});

test("union ordered multiset", () => {
    expect(1+1).toBe(2);    
})